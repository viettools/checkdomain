============
CHECK DOMAIN
============

Docker
======

docker-compose build

docker-compose up -d

docker-compose down

docker container rm <hash>

docker ps -a

docker-compose build --no-cache

docker logs -f --tail 10 <hash>